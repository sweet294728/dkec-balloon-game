{
  "name": "dkec-balloon-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --configLoader runner",
    "export:html": "vite build --configLoader runner && node scripts/export-single-html.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify:production": "vite build --configLoader runner && node tests/verify-production.mjs"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
